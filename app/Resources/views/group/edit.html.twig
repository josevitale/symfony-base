{% extends 'layout.html.twig' %}

{% block contenido %}
    <h1 id="group_edit_titulo">{{ 'group.edit.titulo'|trans }}</h1>
    {{ form_start(form, {'action': path('group_update', { 'id': group.id }), 'method': 'PUT', 'attr': {'class': 'form-horizontal'}}) }}
        <div class="form-group">
            {{ form_label(form.name, 'group.type.name'|trans, {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
            <div class="col-sm-10">
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button id="group_edit_aceptar" type="submit" class="btn btn-primary">{{ 'general.submit'|trans }}</button>
                <a id="group_edit_cancelar" class="btn btn-primary" href="{{ path('group_list') }}" role="button">{{ 'general.cancelar'|trans }}</a>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}
