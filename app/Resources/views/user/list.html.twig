{% extends 'layout.html.twig' %}

{% block contenido %}
    <h1 id="user_list_titulo">{{ 'user.list.titulo'|trans }}</h1>
    <table id="user_list_table" class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>{{ 'user.list.id'|trans }}</th><th>{{ 'user.list.email'|trans }}</th><th>{{ 'user.list.username'|trans }}</th><th>{{ 'general.acciones'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td><td>{{ user.email }}</td><td>{{ user.username }}</td>
                <td class="table_accion">
                    <span>
                       <a class="btn btn-primary btn-sm table_accion_show" href="{{ path('user_show', { 'id': user.id }) }}">{{ 'general.ver'|trans }}</a>
                    </span>
                    <span>
                       <a class="btn btn-primary btn-sm table_accion_edit" href="{{ path('user_edit', { 'id': user.id }) }}">{{ 'general.modificar'|trans }}</a>
                    </span>
                    <span>
                       <a class="btn btn-danger btn-sm table_accion_remove" href="{{ path('user_remove', { 'id': user.id }) }}">{{ 'general.eliminar'|trans }}</a>
                    </span>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <a id="user_list_new" class="btn btn-primary" href="{{ path('user_new') }}" role="button">{{ 'general.agregar'|trans }}</a>
        </div>
    </div>
{% endblock %}
